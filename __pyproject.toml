[tool.poetry]
name = "graphs_rsam1"
version = "0.1.0"
description = "A package implementing graph algorithms including Dijkstra's algorithm."

[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
